# Database

`NordicFlow_ERP.db` is the final Project 4 SQLite database.

Unlike the initial CSV import exercise, this version implements:
- physical primary keys,
- foreign-key relationships,
- basic CHECK constraints,
- analytical indexes,
- two reusable SQL views.

The data is identical in business content to the trusted Project 3 clean dataset.

